{
  "name": "todo-api",
  "type": "service",
  "version": "0.1.0",
  "language": "Ballerina",
  "buildpack": "docker",
  "appPath": "todo-api",
  "entrypoint": "deployment/service",
  "exposure": "intranet",
  "dependencies": [
    { "kind": "platform-resource", "name": "todo-db", "resourceType": "postgres-cnpg" },
    { "kind": "platform-resource", "name": "user-auth", "resourceType": "thunder-app" }
  ],
  "description": "Owns and persists each signed-in user's private todo items (title, completion status) in a dedicated Postgres database, scoped strictly to the caller identified by the gateway's signed assertion. Does not manage user accounts or profiles — those live in Thunder.",
  "stories": [2, 3, 4, 5, 6, 7],
  "skillsPinned": ["openapi-conventions", "ballerina", "thunder-authentication", "api-management"]
}
